pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
                git 'https://github.com/Pritam-Khergade/student-ui.git'
            }
        }
        stage('build') {
            steps {
                sh 'mvn clean install'
            }
        } 
        stage('push') {
            steps {
                sh 'aws s3 cp studentapp-2.2-SNAPSHOT.war s3://jnkin-bkt'
            }
        } 

    }
}
